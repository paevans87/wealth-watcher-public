# Changelog

All notable changes to this project will be documented here.

The project is not yet making public releases. Until the first release, changes are grouped under `Unreleased` and the pull-request history is the detailed record.

## Unreleased

- Prepare the repository for open-source release with public CI, dependency checks, contributor guidance, and safer local deployment defaults.
- Establish the initial public release policy: SemVer beginning at `v0.1.0`, version-aligned UI metadata, release notes, release checks, and application SBOMs; tagged Docker images are published to GHCR while deployment data remains local.
